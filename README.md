# Custom-Dashboard-in-Python

Requirements:

pandas, plotly, dash, dash_bootstrap_components

Commands:

pip install pandas

pip install plotly

pip install dash

pip install dash_bootstrap_components
